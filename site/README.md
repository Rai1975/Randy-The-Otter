# Randy public site

This directory contains the source for Randy's public download and setup page.

The deployment build copies it to `public/`, copies selected extension assets to `public/assets/`, and creates the extension ZIP under `public/downloads/`.
